# Example environment variables (DO NOT commit real secrets)

# Dashboard protection (optional but recommended)
DASHBOARD_TOKEN=change-me-to-a-long-random-token
# Comma-separated allowed origins for WS. If you put dashboard behind HTTPS on your domain, include it here.
DASHBOARD_ALLOWED_ORIGINS=https://dashboard.example.com,http://localhost,http://127.0.0.1
DASHBOARD_MAX_WS_CONNECTIONS=50
DASHBOARD_MAX_WS_MESSAGE_BYTES=32768

# Dashboard bind/port (for run_with_dashboard.py)
DASHBOARD_BIND=127.0.0.1
